// Cấu hình môi trường cho hệ thống
window.APP_CONFIG = {
    // Bước 1: Khi bạn deploy Backend (admin_server.py) lên mạng bằng Render.com, 
    //         Render sẽ cấp cho bạn một đường link HTTPS
    // Bước 2: Hãy copy đường link đó và dán vào giữa cặp dấu nháy đơn phía dưới.
    // Ví dụ: API_BASE_URL: 'https://nbkm-backend.onrender.com'
    // KHÔNG TẠO DẤU / Ở CUỐI ĐƯỜNG LINK

    API_BASE_URL: '' // Tạm thời để trống
};
